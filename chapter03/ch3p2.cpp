#include <iostream>

using namespace std;

int main()
{
    cout << "Summation of two numbers\n enter the first number: ";
    double firstNumber; // defining the variable for taking the value from user
    cin >> firstNumber; // taking the first number from user
    cout<< " Enter the second number: ";
    double secondNumber; // defining the variable for taking the value from user
    cin >> secondNumber; // taking the second number from user
    cout << " the summation of given two numbers is: " << firstNumber + secondNumber << endl; // printing the result of summation
}